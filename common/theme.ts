const color = {
    primary: "#7644FF",
    secondary: "#8DFF75",
};

const size = {
    maxWidth: "1200px",
};

const screen = { maxWidth: "screen and (max-width: 1200px)" };

const theme = {
    color,
    size,
    screen,
};

export default theme;
